import React from "react";
import {Text, View} from 'react-native';

const GameScreen = () => {
    return (
        <View>
        <Text style={{textAlign: 'center', marginTop: '50%', color: '#fff'}}>...Game screen test...</Text>
        </View>
    )
}

export default GameScreen
